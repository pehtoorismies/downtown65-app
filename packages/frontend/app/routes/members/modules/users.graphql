query GetUsers($perPage: Int!, $page: Int!) {
    users(page: $page, perPage: $perPage) {
        users {
            id
            name
            nickname
        }
        length
        limit
        start
        total
    }
}